<div class="wrapper-12 panel-wrapper">
  <span class="pull-right">
    <%= link_to t("links.show"), @job_offer, :class => "btn btn-default", data: {:confirm => t("links.discard")} %>
  </span>
  <h1><%= t("job_offers.edit_job_offer")%></h1>
  <%= render 'form', job_offer: @job_offer %>
</div>
