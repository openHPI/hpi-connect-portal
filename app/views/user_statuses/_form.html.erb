<%= simple_form_for user_status do |f| %>

  <%= render 'shared/object_errors', object: user_status %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
