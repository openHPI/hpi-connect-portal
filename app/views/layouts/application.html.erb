<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "HpiHiwiPortal" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-inverse" role="navigation">
      <div class="navbar-header">
        <%= link_to image_tag("hpi-logo.png", :class=>"logo") + "HpiHiwiPortal", root_path, :class =>"navbar-brand" %>
      </div>
      <div class="container navbar-collapse">
        <ul class="nav navbar-nav">
              <li><%= link_to t('layouts.job_offers'), job_offers_path  %></li>
              <% if not user_is_admin? %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= t('layouts.chairs') %> <b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <% @chairs = Chair.all %>
                      <% @chairs.each do |chair| %>
                      <li><%= link_to chair.name, chair %></li>
                    <% end %>
                  </ul>
                </li>
              <%end %>
              <% if user_is_staff? %>
                <li><%= link_to t('layouts.students'), students_path %></li>
              <%end %>
              <% if user_is_admin? %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown"> <%= t('layouts.overview') %> <b class="caret"></b></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to t('layouts.students'), students_path %></li>
                    <li><%= link_to t('layouts.staff'), staff_index_path %></li>
                    <li><%= link_to t('layouts.chairs'), chairs_path %></li>
                  </ul>
                </li>
              <%end %>
        </ul>
        <p class="navbar-text pull-right">
          <% if user_signed_in? %>
            <%= image_tag(current_user.photo.url, size: "40", class: "img-thumbnail")  %>
            <%= t("layouts.logged_in") %> <strong><%= current_user.email %></strong>.
            <%= link_to t("layouts.my_profile"), edit_user_path(current_user), :class => 'navbar-link' %>
            <% if user_is_staff? && current_user.chair_id%>
              <% @chair = Chair.find(current_user.chair_id) %>
              <%= link_to t('layouts.my_chair'), @chair, :class => 'navbar-link' %>
            <% end %> |
            <%= link_to t("layouts.logout"), destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
          <% else %>
            <%= link_to t("layouts.login"), new_user_session_path, :class => 'navbar-link'  %>
          <% end %>
      </div><!--/.navbar-collapse -->
    </div>

    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <tr>
          <td>&copy; <%= t("layouts.project") %></td>
          <td>|</td>
          <td><%= link_to t("layouts.faqs"), faqs_path %></td>
          <td>|</td>
          <td><%= link_to t("layouts.imprint"), imprint_index_path %></td>
        </tr>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
