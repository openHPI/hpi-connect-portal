<div class="cv-listing">
    <%= l job.start_date, format: '%B %Y' %>
    <% unless job.current %> - <%= l job.end_date, format: '%B %Y' %><% end %><br/>
    <b><%= job.position %></b> at <%= job.employer %><br/>
    <%= simple_format job.description %>
</div>