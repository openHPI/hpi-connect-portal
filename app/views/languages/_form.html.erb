<%= form_for(language) do |f| %>
  
  <%= render 'shared/object_errors', object: language %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
