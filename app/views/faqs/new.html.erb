<div class="wrapper-12 panel-wrapper">
  <h1><%= t("faqs.new_faq") %></h1>

  <% if can?(:create, @faq) %>
    <div class="panel panel-default panel-body">
      <%= render 'form', faq: @faq %>
    </div>
  <% else %>
    <div class="alert alert-danger">
      <h2><%= t("faqs.new_faq_error") %></h2>
    </div>
  <% end %>
</div>